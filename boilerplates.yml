version: 1

file_templates:
  - name: service-dockerignore
    title: Service Docker ignore
    source:
      owner: wodby
      repo: service
      ref: main
    applies_to:
      repo_patterns:
        - "^service-[A-Za-z0-9._-]+$"
    files:
      - path: .dockerignore
        required: false
